<UserControl x:Class="LevelSetsEditor.View.PreviewViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LevelSetsEditor.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical"   >
            <Image  Source="{Binding Source, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" 
                               Height="{Binding Height}"    
                               Width="{Binding Width}"/>
            <TextBox Text ="{Binding StringFormat= Тип превью {0}, Path= Type }" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                <TextBox Text ="{Binding Source, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
                <Button Content="..." Grid.Column="1" Command="{Binding OpenPreviewFileCommand}"/>
            </Grid>            
            <TextBox Text ="{Binding Path= Size, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
        </StackPanel>
    </Grid>
</UserControl>
