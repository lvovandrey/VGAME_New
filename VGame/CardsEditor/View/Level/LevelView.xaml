<UserControl x:Class="CardsEditor.View.Level.LevelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CardsEditor.View.Level"
             xmlns:view ="clr-namespace:CardsEditor.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid >
        <StackPanel Orientation="Vertical">
            <Image Source="{Binding ImageAdressURI}" MaxHeight="200" Height="200"></Image>
            <view:PropertyViewer  DataContext="{Binding Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Tag="Название" Margin="0,0,0,20"/>
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                <view:PropertyViewer Grid.Column="0"  DataContext="{Binding ImageAddress, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Tag="Изображение"/>
                <Button Grid.Column="1" Content="..." Width="30" Command="{Binding OpenImageFileCommand}" />
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
