<UserControl x:Class="LevelSetsEditor.View.PropertyViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LevelSetsEditor.View"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800" x:Name="This" Tag="Default tag">
    <Grid>
        <Grid x:Name="ThisGrid" Tag="Название">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding Path=.}" Grid.Column="1" />
            <Label Content="{Binding ElementName= This, Path=Tag}"/>
        </Grid>
    </Grid>
</UserControl>
